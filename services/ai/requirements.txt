fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3
basic-pitch==0.4.0
librosa==0.11.0
# Deployment/runtime pins to keep madmom compatible.
# - madmom 0.16.x requires NumPy < 1.24 (uses deprecated np.float, removed in 1.24)
# - madmom imports pkg_resources (removed in setuptools 82+)
setuptools>=65,<81
numpy>=1.23,<1.24
scipy>=1.8,<1.14
# Build-time dependency for madmom (install before madmom)
Cython>=0.29.36,<3

# NOTE:
# madmom requires native builds on macOS and has stricter build-time requirements.
# To keep the default install reliable, madmom is moved to requirements-madmom.txt.
